import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Checkout = (props) => {
  // console.log("audiobooks=>", props.route.params.audiobooks);
  return (
    <View>
      <Text>Checkout</Text>
    </View>
  );
};

export default Checkout;

const styles = StyleSheet.create({});
